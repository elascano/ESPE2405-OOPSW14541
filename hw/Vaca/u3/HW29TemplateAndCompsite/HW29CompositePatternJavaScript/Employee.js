class Employee {
    constructor() {
        this.name = "Not assigned yet";
        this.title = "Not assigned yet";
    }

    stateName() {
        console.log(`${this.title} ${this.name}`);
    }
}
