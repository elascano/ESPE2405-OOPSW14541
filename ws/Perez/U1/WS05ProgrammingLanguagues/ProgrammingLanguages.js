function tablaDel2(){
    //recorremos los numeros del 1 al 10
    for(let i=1; i<=10; i++){
        //Calculamos el resultado de la multiplicacion
        let resultado = 2*i;
        //Imprimimos el resulatdo en consola
        console.log(`2x${i} = ${resultado}`);
    }
}